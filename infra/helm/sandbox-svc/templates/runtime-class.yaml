apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
scheduling:
  nodeSelector:
    role: sandbox
  tolerations:
  - key: "sandbox.papertoprod.com/isolated"
    operator: "Exists"
    effect: "NoSchedule"
